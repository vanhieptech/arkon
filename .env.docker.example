# ============================================================
# Arkon — Docker Compose environment
# cp .env.docker.example .env.docker
# ============================================================

# --- PostgreSQL ---
# POSTGRES_* are used by the postgres container; DATABASE_URL must match them
POSTGRES_USER=arkon
POSTGRES_PASSWORD=arkon_secret
POSTGRES_DB=arkon
DATABASE_URL=postgresql+asyncpg://arkon:arkon_secret@postgres:5432/arkon

# --- Auth (JWT) ---
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-random-secret-string

# --- MCP token hashing ---
# HMAC pepper for hashing MCP bearer tokens at rest. Generate with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# Rotating this value invalidates EVERY existing MCP token — set once and keep stable.
MCP_TOKEN_PEPPER=change-me-to-a-random-pepper

# --- Default Admin Account ---
DEFAULT_ADMIN_EMAIL=admin@arkon.local
DEFAULT_ADMIN_PASSWORD=admin123

# --- MinIO ---
MINIO_ENDPOINT=minio:9000
MINIO_PUBLIC_ENDPOINT=localhost:9002
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=arkon-files
MINIO_SECURE=false
MINIO_PRESIGN_EXPIRY_HOURS=24

# --- Redis ---
# Must match docker-compose default (REDIS_PASSWORD:-arkon_secret)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=arkon_secret
REDIS_DB=0

# --- Structured logging (Phase 66 — api/worker/worker_skills) ---
# LOG_FORMAT=json
# LOG_LEVEL=INFO
WORKER_MAX_JOBS=3
WORKER_JOB_TIMEOUT=600

# --- CORS ---
# Production: set to your actual domain, e.g. https://arkon.yourcompany.com
CORS_ORIGINS=*

# --- Frontend ---
# Public URL of the API as seen by the browser
NEXT_PUBLIC_API_URL=http://localhost:5055
